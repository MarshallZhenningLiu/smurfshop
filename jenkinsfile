pipeline {
    agent { label "windows"}

    tools{ maven "maven"
            jdk "java"
            }
    stages {
        stage('Build') {
            steps {
                bat "mvn install"
            }
        }
    
 
    }
}
