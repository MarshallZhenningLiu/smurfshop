pipeline {
    agent { label "windows"}

    tools{ maven "maven"
            jdk "jdk"
            }
    stages {
        stage('Build') {
            steps {
                bat "mvn install"
            }
        }
    
 
    }
}
